coverage:
  status:
    project:
      default:
        target: 95%    # the required coverage value
        threshold: 1%  # the leniency in hitting the target
    patch:
      default:
        target: 95%
        threshold: 1%

comment:
  require_changes: true  # comment only if coverage changed
